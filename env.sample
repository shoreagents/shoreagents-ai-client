# Sample Environment Configuration
# Copy this content to your .env.local file and update with your actual values

# Database Configuration (Required)
DATABASE_URL=your_postgresql_connection_string_here

# Supabase Configuration (if using Supabase)
# NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Redis Configuration (OPTIONAL - for AI chatbot caching)
# Leave these commented out to use in-memory caching (recommended for development)
# The app will work perfectly fine without Redis!
# REDIS_URL=redis://localhost:6379
# REDIS_PASSWORD=

# Claude AI Configuration (Required for AI chatbot)
CLAUDE_API_KEY=sk-ant-api03-your-actual-claude-api-key-here

# OpenAI (for embeddings / RAG). Required if you enable RAG.
# OPENAI_API_KEY=sk-your-openai-key

# Environment
NODE_ENV=development
